{
  "dependencies": {
    "express": "^4.19.2",
    "fetch": "^1.1.0",
    "nodemon": "^3.1.0"
  },
  "scripts": {
    "start": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
